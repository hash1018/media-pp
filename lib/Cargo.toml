[package]
name = "media-pp"
version = "0.1.0"
edition = "2024"

[dependencies]
ffmpeg-next = "8"
crossbeam-channel = "0.5"
thiserror = "2"
renderer-engine = { git = "https://github.com/hash1018/RendererEngine", package = "renderer-engine", optional = true }

[features]
dx12-renderer = ["dep:renderer-engine"]
