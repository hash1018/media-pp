[package]
name = "screen_overlay_record"
version = "0.1.0"
edition = "2024"
publish = false

[target.'cfg(target_os = "linux")'.dependencies]
media-pp = { path = "../../../lib", features = [
    "pipewire-screen-capture",
    "cuda",
] }
