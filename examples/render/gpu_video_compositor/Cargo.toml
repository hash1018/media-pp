[package]
name = "gpu_video_compositor"
version = "0.1.0"
edition = "2024"
publish = false

[target.'cfg(windows)'.dependencies]
media-pp = { path = "../../../lib", features = ["d3d11-renderer"] }
ffmpeg-next = "8"
render_common = { path = "../render_common" }
winit = "0.30"
