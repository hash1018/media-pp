[package]
name = "screen_capture"
version = "0.1.0"
edition = "2024"
publish = false

[target.'cfg(windows)'.dependencies]
media-pp = { path = "../../../lib", features = ["d3d12-renderer", "dxgi-capture"] }
ffmpeg-next = "8"
render_common = { path = "../render_common" }
winit = "0.30"
