[package]
name = "d3d11_chroma_key"
version = "0.1.0"
edition = "2024"
publish = false

[target.'cfg(windows)'.dependencies]
media-pp = { path = "../../../lib", features = ["d3d11"] }
render_common = { path = "../render_common" }
