[package]
name = "webrtc_loopback"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
media-pp = { path = "../../../lib", features = ["webrtc"] }
str0m = { version = "0.21", default-features = false, features = ["rust-crypto"] }
